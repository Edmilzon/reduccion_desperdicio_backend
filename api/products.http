# ============================================
# PRODUCTS - Productos con Categorías
# ============================================
# Los productos pertenecen a un comercio y tienen categoría.
# Base URL: http://localhost:3000

@baseURL = https://reduccion-desperdicio-backend.vercel.app/
@token = your_jwt_token_here
@commerceId = 1
@productId = 1
@categoryId = 1
@categorySlug = panaderia

# ============================================
# GET - CONSULTAS
# ============================================

# Listar todos los productos con su categoría (sin filtros)
GET {{baseURL}}/products/all

# Buscar productos por título, descripción o nombre del comercio
GET {{baseURL}}/products/search?q=pan

# Listar todos los productos (filtros: categoryId, commerceId)
GET {{baseURL}}/products?categoryId={{categoryId}}&commerceId={{commerceId}}

# Obtener productos por comercio (filtros: status, categoryId)
GET {{baseURL}}/products/commerce/{{commerceId}}?status=active&categoryId={{categoryId}}

# Obtener producto por ID
GET {{baseURL}}/products/{{productId}}

# --- Categorías ---

# Listar todas las categorías (solo categorías, sin productos)
GET {{baseURL}}/products/categories

# Obtener productos por categoría
GET {{baseURL}}/products/category/{{categoryId}}?commerceId={{commerceId}}

# ============================================
# POST - CREAR
# ============================================

# Crear producto (requiere autenticación)
POST {{baseURL}}/products
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "title": "Pan Integral",
  "description": "Pan integral fresco",
  "originalPrice": 5.00,
  "price": 3.50,
  "quantity": 20,
  "pickupStart": "2024-12-31T08:00:00Z",
  "pickupEnd": "2024-12-31T14:00:00Z",
  "commerceId": 1,
  "categoryId": 1,
  "imageUrl": "https://ejemplo.com/imagen.jpg"
}

# ============================================
# PATCH - ACTUALIZAR
# ============================================

# Actualizar producto (requiere autenticación)
PATCH {{baseURL}}/products/{{productId}}
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "title": "Pan Integral Actualizado",
  "price": 4.00,
  "quantity": 15
}

# ============================================
# DELETE - ELIMINAR
# ============================================

# Eliminar producto (soft delete, requiere autenticación)
DELETE {{baseURL}}/products/{{productId}}
Authorization: Bearer {{token}}